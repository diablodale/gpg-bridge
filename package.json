{
  "name": "gpg-windows-relay-monorepo",
  "description": "GPG Windows Relay - Three extension pack for relaying GPG to Windows from remotes",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "npm run build:bridge && npm run build:remote",
    "build:bridge": "cd bridge && npm run compile",
    "build:remote": "cd remote && npm run compile",
    "watch": "npm run watch:bridge & npm run watch:remote",
    "watch:bridge": "cd bridge && npm run watch",
    "watch:remote": "cd remote && npm run watch",
    "package": "npm run package:bridge && npm run package:remote && npm run package:pack",
    "package:bridge": "cd bridge && npm run package",
    "package:remote": "cd remote && npm run package",
    "package:pack": "cd pack && npm run package",
    "clean": "rimraf -g \"bridge/out\" \"remote/out\" \"bridge/gpg-windows-relay-bridge-*.vsix\" \"remote/gpg-windows-relay-remote-*.vsix\" \"pack/gpg-windows-relay-*.vsix\""
  },
  "devDependencies": {
    "rimraf": "^5.0.0"
  }
}
